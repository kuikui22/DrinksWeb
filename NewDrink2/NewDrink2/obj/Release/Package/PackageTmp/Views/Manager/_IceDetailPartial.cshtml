@using NewDrink2.Models
@model IEnumerable<NewDrink2.Models.IceType>

@foreach (var item in Model)
{
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IceName)
        </td>
    </tr>
}
